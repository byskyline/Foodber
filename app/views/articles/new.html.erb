<%= form_for @article, :url => articles_path, :class => "form" do |f|%>
	<%= f.label :topic, "Topic: " %>
	<%= f.text_field :topic  %>
	<%= f.label :content, "Content: " %>
	<%= f.text_area :content %>
	<%= f.submit "Create"%>
<% end %>